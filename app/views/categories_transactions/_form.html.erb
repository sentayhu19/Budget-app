<%= form_with(model: categories_transaction) do |form| %>
  <% if categories_transaction.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(categories_transaction.errors.count, "error") %> prohibited this categories_transaction from being saved:</h2>

      <ul>
        <% categories_transaction.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :transactions_id, style: "display: block" %>
    <%= form.text_field :transactions_id %>
  </div>

  <div>
    <%= form.label :categories_id, style: "display: block" %>
    <%= form.text_field :categories_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
